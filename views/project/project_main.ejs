<% include partial_promenu %><p>

  <script>
  $('#project_menu a:contains("Resumen")').parent().addClass("active");
  </script>

<h2 class="page-header">
Portada
</h2>

<h3 class="page-header">
  Tareas Generales
</h3>

<table class="table table-striped table-condensed">
<% var i; if (tasks_all) { for (i=0; i < tasks_all.length; i++){ %>
	<tr>
	  <td><%= tasks_all[i].tas_tarea %></td>
  </tr>
<% }} %>
</table>

<h3 class="page-header">
  Mis Tareas
</h3>

<table class="table table-striped table-condensed">
<% var i; if (tasks) { for (i=0; i < tasks.length; i++){ %>
	<tr>
	  <td><%= tasks[i].tas_tarea %></td>
  </tr>
<% }} %>
</table>
